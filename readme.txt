HTML5 Boilerplate: Travis's Fork

HTML:
* XHTML-compliant
* additional meta and page info added

JS:
* main.js has default JS

CSS:
* reset.css included, but not linked
* mobile unlinked

misc:
* added cache manifest, updated web.config
* removed a bunch of unused (or very rarely used) files and directories
